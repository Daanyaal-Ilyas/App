<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:App.Pages"
             x:Class="App.Pages.Profile">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" Padding="20" Spacing="10">
                <Label Text="Profile" FontSize="Large" HorizontalOptions="Center" />
                <Label x:Name="NameLabel" Text="Name" />
                <Label x:Name="EmailLabel" Text="Email" />
                <Label x:Name="UsernameLabel" Text="Username" />
            </StackLayout>

            <ListView x:Name="UserPostsListView" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Title}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>

