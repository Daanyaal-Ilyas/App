<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:App.Pages"
             x:Class="App.Pages.Profile"
             BackgroundColor="{StaticResource PrimaryColor}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource SecondaryColor}"/>
                <Setter Property="FontFamily" Value="Roboto"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" Padding="20" Spacing="10">
                <Label Text="Profile" FontSize="Large" TextColor="{StaticResource AccentColor}" FontAttributes="Bold" HorizontalOptions="Center" />
                <Label x:Name="NameLabel" Text="Name" TextColor="{StaticResource AccentColor}" />
                <Label x:Name="EmailLabel" Text="Email" TextColor="{StaticResource AccentColor}" />
                <Label x:Name="UsernameLabel" Text="Username" TextColor="{StaticResource AccentColor}" />
            </StackLayout>
            <StackLayout Grid.Row="1" Margin="20">
                <Label Text="Post History" FontSize="Large" TextColor="{StaticResource AccentColor}" FontAttributes="Bold" Margin="0,0,0,10" />
                <ListView x:Name="PostsListView" ItemSelected="PostsListView_ItemSelected" SeparatorVisibility="Default" SeparatorColor="{StaticResource SecondaryColor}" RowHeight="75">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Padding="10,5,10,5"  BackgroundColor="{StaticResource PrimaryColor}">
                                    <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource AccentColor}" />
                                    <Label Text="{Binding Description}" FontSize="14" LineBreakMode="TailTruncation" MaxLines="1" TextColor="{StaticResource AccentColor}" />
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>